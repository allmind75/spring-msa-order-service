server:
  port: 9200

spring:
  application:
    name: order-service
  profiles:
    active: local
  cloud:
    config:
      uri: http://localhost:8500

application:
  project:
    name: order-service

logging:
  path:
    root: ./logs